## Menu bar and Dock fixes

- The menu bar icon now uses native template rendering, adapting its color to the system menu bar.
- Dock visibility synchronizes AppKit activation policy and process presentation state, outside click handling.

Update: `brew update` then `brew upgrade --cask demureiskander/dropzone/demureiskander-dropzone`.

Apple Silicon, macOS 13+. Apache-2.0. Ad-hoc signed, not notarized.
